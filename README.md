# RAG-ingestion-Pipeline
A simple end-to-end RAG ingestion pipeline built using LangChain that loads text documents, splits them into chunks, generates embeddings using HuggingFace, and stores them in a local Chroma vector database for efficient retrieval.
